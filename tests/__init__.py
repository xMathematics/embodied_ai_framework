"""
================================================================================
 测试包 (tests)
 ──────────────────────────────────────────────────────────────────────────────
 本包包含项目中所有模块的单元测试和集成测试。

 测试策略 (Test Strategy):
   ┌──────────────────────────────────────────────────────────┐
   │                   测试金字塔 (Test Pyramid)              │
   │                                                          │
   │                    ╱╲                                    │
   │                   ╱  ╲   E2E / 集成测试 (少量)          │
   │                  ╱    ╲                                  │
   │                 ╱──────╲                                 │
   │                ╱ 服务/API ╲  服务层测试                  │
   │               ╱────────────╲                             │
   │              ╱              ╲                            │
   │             ╱────────────────╲                           │
   │            ╱  单元测试 (大量)   ╲  核心逻辑测试          │
   │           ╱──────────────────────╲                       │
   └──────────────────────────────────────────────────────────┘

 分层测试策略:
   - 单元测试: 每个类的核心逻辑单独测试，使用 Mock 隔离依赖
   - 集成测试: 测试模块间的交互 (如 DataLoader ↔ Adapter)
   - E2E 测试: 完整的训练流水线测试 (运行时间较长)

 测试运行:
   运行全部测试:       pytest tests/ -v
   运行特定层测试:     pytest tests/data/ -v
   运行特定文件测试:   pytest tests/test_common.py -v
   生成覆盖率报告:     pytest tests/ --cov=src --cov-report=html

 测试命名规范:
   - 测试文件: test_<module_name>.py
   - 测试类:   Test<ClassName>
   - 测试方法: test_<method_name>_<scenario>
================================================================================
"""
