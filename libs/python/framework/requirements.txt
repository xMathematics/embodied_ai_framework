# ═══════════════════════════════════════════════════════════════════════════════
# 依赖列表 (requirements.txt)
# ──────────────────────────────────────────────────────────────────────────────
# 安装命令: pip install -r requirements.txt
#
# 可选依赖:
#   mujoco>=3.0         MuJoCo 仿真引擎
#   pybullet>=3.2       PyBullet 仿真引擎
#   s3fs>=2023.0        S3 对象存储支持
#   opencv-python       视频录制支持 (评估时渲染)
# ═══════════════════════════════════════════════════════════════════════════════

# ── 核心依赖 ─────────────────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15
numpy>=1.24
pyarrow>=12.0
h5py>=3.8
datasets>=2.10
PyYAML>=6.0
Pillow>=10.0
mlflow>=2.3
psutil>=5.9

# ── 开发依赖 (可选) ───────────────────────────────────────────────────────────
pytest>=7.0
black
flake8

# ── 仿真引擎 (可选) ──────────────────────────────────────────────────────────
mujoco>=3.0
pybullet>=3.2

# ── 额外功能 (可选) ──────────────────────────────────────────────────────────
s3fs>=2023.0
opencv-python
