"""
================================================================================
 数据层 (Data Layer)
 ──────────────────────────────────────────────────────────────────────────────
 五层架构中的第二层，负责所有与数据相关的操作：
   - registry.py:      数据集注册中心，管理所有可用数据集的元信息和版本
   - adapters/:        数据集适配器，将不同源格式转换为统一格式
   - standardization.py: 数据标准化与统一中间格式定义
   - augmentation.py:  数据增强流水线 (图像级 + 轨迹级)
   - dataloader.py:    数据供给接口，连接数据层到算法层训练循环

 数据流路径:
   原始数据(异构格式) → DatasetAdapter → 统一格式(Arrow/IPC) → 增强 → DataLoader → 训练循环
================================================================================
"""
