"""
================================================================================
 基础设施层 (Infra Layer)
 ──────────────────────────────────────────────────────────────────────────────
 这是五层架构的最底层，提供所有上层模块依赖的基础设施能力：
   - compute.py:  计算资源调度管理 (GPU/CPU 分配、分布式训练支持)
   - storage.py:  存储管理层 (数据集、模型权重、日志的统一存储抽象)
   - container.py: 容器化部署支持 (Docker/K8s 环境管理)

 设计原则:
   基础设施层对各上层模块完全透明——上层通过配置指定计算和存储需求，
   由本层自动完成资源分配和调度。
================================================================================
"""
